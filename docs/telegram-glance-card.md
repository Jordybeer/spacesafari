# Telegram glance card

`/wie` is intentionally optimized for festival use on a phone: one two-line block per active stage, no repeated full country names, and the next set inline on the metadata line. `/programma` remains the detailed metadata view.
